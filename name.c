#include<stdio.h>
int main()
{
    char name [1][20];
    printf("enter any name");
    for (int i = 0; i < 1; i++)
    {
        scanf("\n%s",&name[i]);
    }
    for (int i = 0; i < 1; i++)
    {
        printf("%s",name[i]);
    }
      printf("%s",name);
        printf("%s",name);
    return 0;
    
}